# Normative Rule Registry

Machine-readable language rules live here, grouped by language version and
surface. A rule ID is permanent once published. Rule files are normative only
when their status is `stable`; experimental rules may change without a
compatibility guarantee.

Each stable rule must identify positive and negative conformance evidence.
Rules with runtime effects must also identify runtime evidence.
